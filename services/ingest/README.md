# HaulDesk Ingest (Rust)

Streaming ELD/telematics ingestion. Feeds location/HOS events into Postgres via the Go API.

## Status

Stub — `cargo run` prints a placeholder message.

```bash
cd services/ingest
cargo build
cargo run
```
