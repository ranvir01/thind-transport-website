module github.com/thind-transport/hauldesk/services/api

go 1.22

// Core TMS API — loads, dispatch, billing, auth/tenancy.
// Implements hauldesk.v1 gRPC services and REST /v1/* for the Next.js BFF.
// Postgres is the system of record.
