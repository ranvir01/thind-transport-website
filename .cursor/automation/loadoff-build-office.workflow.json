{
  "name": "LoadOff Build A — office/UX",
  "description": "Daily 05:13 UTC — one office-territory item per run on claude/lane-office. Grok 4.6.",
  "workflow": {
    "triggers": [
      {
        "cron": {
          "cron": "13 5 * * *"
        }
      }
    ],
    "actions": [],
    "prompts": [
      "You are the LoadOff office/UX scheduled builder (not a Dex Fire Cursor start).\n\nRead AGENTS.md, docs/cursor-agent-preamble.md, and .cursor/automation/loadoff-build-office.prompt.md for full steps.\n\nCursor starts you on a disposable cursor/* branch: npm run git:identity, npm run hooks:install, git fetch origin, git checkout -B claude/lane-office (from origin/claude/lane-office if it exists, else origin/main), merge origin/main.\n\nTerritory: src/app/hub/(office)/** and office components in src/components/hub/* only. npm run agent:status first (catch-up = assist the drain). Intake: collaborator-labeled should issues in territory first (land with Closes #N), else agent:backlog, else lane mission. Dedupe claude/* AND open cursor/* PRs — same ticket never on this lane and a Fire Cursor agent and the Claude 9-task fleet. Ship ONE item with a test, npm run build && npx vitest run green, push claude/lane-office, Backlog: trailer. Never merge. Never push main.\n\nRepo: ranvir01/thind-transport-website."
    ],
    "gitConfig": {
      "repo": "ranvir01/thind-transport-website",
      "branch": "claude/lane-office"
    },
    "model": "cursor-grok-4.6-high-fast",
    "agentOptions": {
      "skipInstall": false
    },
    "memoryEnabled": true
  }
}
