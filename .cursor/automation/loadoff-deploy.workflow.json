{
  "name": "LoadOff deploy + backlog",
  "description": "Hourly at :59 UTC — drains integrator to main when behind, then ships one ranked Backlog item when caught up.",
  "workflow": {
    "triggers": [
      {
        "cron": {
          "cron": "59 * * * *"
        }
      }
    ],
    "actions": [],
    "prompts": [
      "You are the LoadOff deploy + backlog background agent (docs/agent-improvement-loop.md section 3a).\n\nRead AGENTS.md and .cursor/automation/loadoff-deploy.prompt.md for full steps.\n\nRun npm run agent:status first. Phase A (catch-up): while integrator is ahead of main, merge claude/hauldesk-project-setup-l1luoo to main with full verify, push, stop. Phase B (steady): when caught up, npm run agent:backlog, ship TOP item only, verify, push.\n\nOne commit per run. End bodies with Backlog:.\n\nRepo: ranvir01/thind-transport-website, branch main."
    ],
    "gitConfig": {
      "repo": "ranvir01/thind-transport-website",
      "branch": "main"
    },
    "model": "cursor-grok-4.6-high-fast",
    "agentOptions": {
      "skipInstall": false
    },
    "memoryEnabled": true
  }
}
