# Weekly rollup — 2026-07-25 (Fri)

First run. Section 3 audit only; no feature code was written, by design.

**Ranked actions live in [`TOP_10.md`](./TOP_10.md).** This file is the Friday repo-health slice and the
week-over-week baseline everything after this is measured against.

---

## Repo health

| Metric | Value | Source |
|---|---|---|
| Tests | 189 files / 1592 tests, all passing | `npx vitest run` (68s) |
| Typecheck (app code) | 0 errors | `npx tsc --noEmit`, filtered to non-test paths |
| Typecheck (test files) | 108 diagnostics across 32 files | vitest never typechecks these — TEST_GAPS §3 |
| `npm run build` | Fails in sandbox only (blocked egress to `fonts.googleapis.com`) | Environment limit, not a repo defect |
| `npm run connections:check` | Passes | 10 providers wired, 0 credentialed |
| Remote branches | 232 total / 200 unmerged / 103 zero-diff | `git branch -r --no-merged origin/main` |
| Production errors (7d) | 1 group, benign `pg` SSL deprecation on cron | Vercel runtime errors |
| Latest production deploy | READY, `thindtransport.com` | Vercel `get_project` |

## Test coverage — where it is thin and it matters

Money-critical modules with **0% coverage**: `draftSettlements` (`settlements.ts:89-235`, the whole weekly
payroll path), 12 of 14 money server actions, `createLoad`/`updateLoad`, the advance-apply loop, the 1099-NEC
export. The 49 e2e smoke scripts in `scripts/e2e-*.mjs` are **not in CI** — nothing in `.github/workflows/`
runs them; they execute only when an agent remembers to. Full ranking in `TEST_GAPS.md` §2.

Cheapest test worth writing first: `draft-settlements-loads.test.ts`, asserting the `settlement_id` stamp at
`settlements.ts:218`. 20 minutes; it is the only thing stopping a load being paid twice.

## Spend

Measured software spend today: **$0/mo**. Vercel Hobby, Anthropic key unset, zero blob-backed documents, zero
integration credentials. Realistic post-go-live: **$20–45/mo**. Carrier operations at 12 trucks: **~$16–17k/mo
at list**, roughly 90% insurance + factoring. Every operations figure is list price, not your invoice — see
`RUN_COST.md` for the MISSING list.

## Against the two missions

**A — SHIP.** Money path, settlements, dispatch, driver PWA and compliance rate Ship. Auth/RBAC, invoicing,
fuel+IFTA, doc-intake, customer portal and the integrations registry rate Gaps. Roughly 30 engineering hours
closes every Gaps rating. The under-2-hours onboarding target is currently unmeasurable —
`docs/onboarding-runbook.md` does not exist, and ATS Transport is not in the database at all.

**B — EARN.** Both levers you named are the right ones and neither is instrumented. Deadhead is a seed
constant; DSO is measured on the wrong clock. The audit's contribution this week is that the first step on
both is cheaper than expected: real deadhead comes out of one month of fuel-card CSV, and the full cash cycle
is already reconstructible from `hub.load_events`.

## Next Friday, re-rank against

1. Did the fuel-card CSV land, and what is real deadhead?
2. Are documents private in production?
3. How many delivered-but-unbilled loads are left in the real book?
4. Branch count — 200 unmerged today. Anything above 200 next week means the prune fix did not land.
5. Test coverage on the five money paths named above.

```
FILES:    docs/ops/weekly-2026-07-25.md (created)
PR:       none (no GitHub write access this session)
IMPACT:   Baseline set — 1592 tests green, 200 unmerged branches, $0/mo software spend,
          and two EARN levers confirmed unmeasured rather than merely underperforming
NEXT:     Land the seven docs/ops files (patch attached), then item #1 in TOP_10.md
BLOCKED:  GitHub write access; one month of fuel-card CSV
```
