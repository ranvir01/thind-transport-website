# Credentials shopping list — what to buy, what it activates

Ranked by value-per-dollar for a 15-truck carrier. Paste keys into
Settings → Integrations; each row activates a finished, contract-tested
adapter — or documents exactly what's still stub while the owner waits on
vendor feed setup. Kept current by the integrations lane every time an
adapter ships (see `docs/integrations/README.md` step 8).

| # | Provider | Plan / est. cost | Get it | Activates | Adapter status |
|---|---|---|---|---|---|
| 1 | Terminal (TruckX ELD) | Terminal aggregator — free/dev tier to start | withterminal.com | Live map positions, HOS clocks on dispatch, auto-IFTA miles | **live — ready today** |
| 1b | TruckerCloud ELD | TruckerCloud developer account — Client ID + secret | docs.truckercloud.com | Same as Terminal, via a different aggregator — `activeTelematicsSource()` prefers Terminal, falls back to TruckerCloud | **adapter shipped** (`src/lib/hub/telematics.ts` `truckerCloudSource`, OAuth2 client-credentials); confirm the real token endpoint + feed shape once a contact replies (see `docs/integrations/truckercloud.md`) |
| 2 | Docs mailbox (IMAP) | Free (use an existing mailbox: Gmail app password, or an Entra ID app registration / Google service account for OAuth2) | Gmail app-password settings, or M365/Workspace admin (setup steps in `docs/integrations/mailbox.md`) | Rate cons/PODs auto-file to the matching load by subject reference | **live for Gmail (app password), Microsoft 365, and Google Workspace (both via OAuth2/XOAUTH2, `src/lib/hub/mailbox-oauth.ts`, shipped 2026-07-11)** |
| 3 | EFS fuel feed | Included with the fuel card account — ask your rep for **data-feed** credentials (separate from the portal login); allow ~5 business days to provision | efsllc.com account rep | Daily fuel transactions land in `fuel_transactions` (same table + idempotency the CSV import uses) → MPG, fraud flags, fuel↔load linking, no more manual export/import | **live — ready today** (`src/lib/hub/integrations/efs.ts`); the real delivery is a daily SFTP CSV (no REST), so activation goes through the **signed file-drop URL** on the EFS card — any forwarder POSTs the daily file to `/api/hub/webhooks/efs` (shipped 2026-07-17, see `docs/integrations/efs.md`); column layout still unverified until a provisioned file lands |
| 3b | WEX fuel feed | Included with the card account — sign WEX **Data Release Forms** via your rep (800-492-0669); have the 13-digit WEX account number ready; allow 7–10 business days (some account prefixes +7) | wexinc.com account rep | Same idempotent fuel-transaction ingest as EFS | **live — ready today** (`src/lib/hub/integrations/wex.ts`, cron `wex-sync`); the real delivery is a daily SFTP CSV (confirmed 2026-07-18, same as EFS — no REST), so activation goes through the **signed file-drop URL** on the WEX card — any forwarder POSTs the daily file to `/api/hub/webhooks/wex` (see `docs/integrations/wex.md`); column layout still unverified until a provisioned file lands |
| 4 | Comdata fuel feed | Ask your account team for API credentials | comdata.com | Same idempotent fuel-transaction ingest as EFS | **live — ready today** (`src/lib/hub/integrations/comdata.ts`, cron `comdata-sync`); confirm real endpoint/auth-header names against `docs/integrations/comdata.md` once the account team's onboarding response comes back |
| 5 | DAT load board | DAT One/Power + API access, certification required | dat.com | In-app freight search → one-click book, straight onto the load board | **feature complete** — search client + `datPostingToLoadDraft` mapper (`src/lib/hub/integrations/dat.ts`), dispatcher search UI + lane/equipment filters + "Book" button with customer picker (`LoadBoardFreightSearch.tsx` on `/hub/loadboard`), `bookDatPostingAction` calling `createLoad()` (`src/app/hub/_actions/dat-freight.ts`), all mock+contract+action tested — see `docs/integrations/dat.md`; `registry.ts` already `status: "live"` (manual sync) since dispatchers can search/book once credentials are pasted — only remaining gap is confirming the real DAT auth/response shape against a developer packet |
| 6 | QuickBooks Online | QBO plan + free Intuit developer app | developer.intuit.com | Pulls QBO payments onto matching invoices (by number); pushes our invoices into QBO with a matching `DocNumber` (customer/item resolved by name, no migration) — both directions through/around the same `recordPayment` path the office UI uses — status, audit, load-status cascade all included | **adapter shipped both directions, Push to QBO button wired** — `src/lib/hub/integrations/qbo.ts` (`runQboSync` pull + `pushInvoiceToQbo` push), refresh-token rotation persisted, contract tested (no migration needed — see `docs/integrations/qbo.md`); confirm the real sandbox response shape before flipping to live |
| 7 | Factoring company API | Ask your factor (varies) | your factor | Electronic invoice submission (`submitInvoiceToFactor`) + advance/reserve tracking, applied through the SAME `recordPayment` path the office UI uses | **feature complete, webhook path live-driven end to end** (2026-07-17 rig drive, 38 checks: HMAC verify, inline funding, duplicate replay, park-before-invoice → "Retry N events" drains it) — push + webhook-receiver slice in `src/lib/hub/integrations/factor.ts` (no migration needed, same `sent_log`/`reference`-column dedup route as QBO — see `docs/integrations/factor.md`), plus a "Submit to factor" button on the invoice detail page (`MoneyActions.tsx`, gated on `factorConnected`/`factorSubmitted`) wired to `submitInvoiceToFactorAction`; only remaining gap is confirming the real vendor request/webhook shape before flipping to live |
| 8 | Truckstop.com load board | Truckstop.com developer API key | truckstop.com | Second in-app freight search, same booking UI as DAT | **feature complete** — search client + `truckstopPostingToLoadDraft` mapper (`src/lib/hub/integrations/truckstop.ts`), `'truckstop'` added to `hub.loads.source`'s CHECK constraint (`migrations/hub/017_truckstop_load_source.sql`, already applied), the same `LoadBoardFreightSearch.tsx` tab/booking UI DAT uses, `bookTruckstopPostingAction` (`src/app/hub/_actions/truckstop-freight.ts`), all mock+contract+action tested — see `docs/integrations/truckstop.md`; `registry.ts` already `status: "live"` (manual sync) — only remaining gap is confirming the real developer-packet auth/response shape |

Free already active or key-only (no adapter needed — see the "Free government
APIs" panel on the Integrations page): FMCSA QCMobile (broker vetting), EIA
diesel index, OSRM routing, Nominatim geocoding, NWS weather, NHTSA VIN.
