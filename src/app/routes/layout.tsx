/**
 * No metadata here.
 *
 * This layout used to export a full metadata block that the page's own export
 * already overrode — title, description, OG and Twitter, plus fifteen keywords
 * and a "rates up to $2.81/mi" figure repeated three times that appears in no
 * constant and matches no published rate on the site. The page owns its
 * metadata; the canonical moved there with it.
 */
export default function RoutesLayout({ children }: { children: React.ReactNode }) {
  return children
}
